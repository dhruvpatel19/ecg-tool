# ECG Platform Spec

The canonical project specification for this workspace is stored at:

`../ECG_PLATFORM_SPEC.md`

The implementation and supporting docs in this folder were built from that root specification. The spec was found at the repository root rather than under `docs/` when the workspace was first inspected.
