# Foundations is implemented by the native application runtime. The retired
# static prototype must never be copied beneath `public/`; the predev/prebuild
# boundary check fails closed if that directory reappears.

# Playwright E2E artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/.last-run.json
